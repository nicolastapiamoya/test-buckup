/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
        css: true, // Esto puede variar según la versión de Next.js
    }
};

export default nextConfig;
